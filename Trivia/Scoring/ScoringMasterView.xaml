<UserControl x:Class="Trivia.Scoring.ScoringMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Trivia.Scoring"
             mc:Ignorable="d" 
             d:DesignHeight="737" d:DesignWidth="716">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Placeholder for Round Info" FontSize="26" />
        <Label Grid.Row="1" Content="Scoring" FontSize="26" />
        <Button Grid.Row="1" Content="Score Next Round" Command="{Binding AutoScoreNextRoundCommand}" Width="100" Height="40" />
    </Grid>
</UserControl>
