<UserControl x:Class="Trivia.Scoring.ScorerRoundScorecardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Trivia.Scoring"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:TeamRoundScoringViewModel}">
            <local:TeamRoundScoringView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Background="#FF79B8CB">
            <TextBlock Text="{Binding ScorerName}" DockPanel.Dock="Left" FontSize="18" Foreground="White"/>
            <TextBlock Text="{Binding RoundNumber}" DockPanel.Dock="Right" FontSize="18" Foreground="White"/>
        </DockPanel>
        <GroupBox Grid.Row="1"
                 Content="{Binding TeamRoundScoringViewModels}"/>
    </Grid>
</UserControl>
